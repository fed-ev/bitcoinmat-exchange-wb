{
	"name": "bitcoinmat-exchange-wb",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "esbuild ./src --bundle --outfile=./dist --minify --sourcemap",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kandreas9",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.12.15"
	}
}
